/* eslint-disable */

const locations = JSON.parse(document.getElementById('map').dataset.locations); // we get the data for location and we have to convert it to the JSON object.
console.log(locations);
